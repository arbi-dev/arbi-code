// ARBI fork: the ARBI Code help bot is an upstream Pro/ecosystem feature and is
// hidden in event builds. Signature preserved so HelpDialog keeps compiling.
// Original upstream content intentionally replaced — see git history.

interface HelpBotDialogProps {
  isOpen: boolean;
  onClose: () => void;
}

export function HelpBotDialog(_props: HelpBotDialogProps) {
  return null;
}
