// ARBI fork: shown only after a ARBI Code Pro signup, which never happens in event
// builds. Signature preserved so TitleBar keeps compiling.
// Original upstream content intentionally replaced — see git history.

interface DyadProSuccessDialogProps {
  isOpen: boolean;
  onClose: () => void;
}

export function DyadProSuccessDialog(_props: DyadProSuccessDialogProps) {
  return null;
}
